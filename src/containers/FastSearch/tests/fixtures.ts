import { sampleResponse } from '../../../services/BackendApi/tests/fixtures/sampleResponse';
import { initialState } from 'store/modules/fastSearch/model';

export const serachResultsFixture = sampleResponse.data.hotels;
export const initialStateFixture = initialState;
