import styled from 'styled-components';
import { FastSearchProps } from './index';

const StyledFastSearchContainer = styled.div`
  border: red 10px solid;
`;

export default StyledFastSearchContainer;
